import React from "react";
import "./projects.css";
import CTA from "./CTA";

const projects = () => {
  return (
    <section id="projects">
      <CTA />
    </section>
  );
};

export default projects;
